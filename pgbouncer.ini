# Pooling settings are maintained here; docker-compose mounts this file read-only.
# The two application roles together have at most 80 server connections.
################## Auto generated ##################
[databases]
* = host=db port=5432 auth_user=postgres
[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 5432
unix_socket_dir = 
user = postgres
auth_file = /etc/pgbouncer/userlist.txt
auth_type = scram-sha-256
pool_mode = transaction
max_client_conn = 500
default_pool_size = 15
min_pool_size = 1
reserve_pool_size = 5
ignore_startup_parameters = extra_float_digits

# Log settings
admin_users = postgres

# Connection sanity checks, timeouts

# TLS settings

# Dangerous timeouts
################## end file ##################

max_db_connections = 20

max_user_connections = 20

server_idle_timeout = 60

[users]
postgres = max_user_connections=60
codespor = max_user_connections=20
